name: QM38 hierarchical meta artifact build

on:
  pull_request:
    branches: [main]
    paths:
      - 'qm38_remote/**'
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
      - name: Set up Python
        uses: actions/setup-python@42375524e23c412d93fb67b49958b491fce71c38
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: qm38_remote/requirements-ci.txt
      - name: Install pinned runtime
        run: |
          python -m pip install --upgrade pip==25.1.1
          python -m pip install -r qm38_remote/requirements-ci.txt
      - name: Build deterministic QM38 package
        id: build
        shell: bash
        run: |
          set +e
          python qm38_remote/run_qm38.py > qm38_build.log 2>&1
          code=$?
          cat qm38_build.log
          echo "code=$code" >> "$GITHUB_OUTPUT"
          exit 0
      - name: Publish build diagnostic to PR
        if: steps.build.outputs.code != '0'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          printf 'QM38 build diagnostic:\n\n```text\n%s\n```\n' "$(tail -c 6000 qm38_build.log)" > qm38_comment.md
          gh pr comment "${{ github.event.pull_request.number }}" --body-file qm38_comment.md
      - name: Fail after diagnostic
        if: steps.build.outputs.code != '0'
        run: exit 1
      - name: Run package acceptance tests
        run: python qm38_remote/output/FINAL_QM38/tests/test_qm38_outputs.py qm38_remote/output/FINAL_QM38
      - name: Upload FINAL_QM38
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: FINAL_QM38
          path: qm38_remote/output/FINAL_QM38/
          if-no-files-found: error
          retention-days: 7
          compression-level: 9
