# Quickstart

```bash
pip install -e .
python -m materials_evidence_stack.demo
pytest
```

The demo uses a synthetic fixture so the repository can run without private PDFs, paid datasets, or API keys.
